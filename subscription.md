# Django + Stripe Subscription System Documentation

এই সাবস্ক্রিপশন সিস্টেমটি Django REST Framework এবং Stripe ব্যবহার করে তৈরি করা হয়েছে। নিচে এর ব্যবহারবিধি এবং বিস্তারিত তথ্য দেওয়া হলো।

## ১. এপিআই এন্ডপয়েন্ট (API Endpoints)

| এন্ডপয়েন্ট | মেথড | ডেসক্রিপশন |
| :--- | :--- | :--- |
| `/api/v1/subscriptions/plans/` | `GET` | সব সাবস্ক্রিপশন প্ল্যান দেখার জন্য। |
| `/api/v1/subscriptions/checkout/` | `POST` | নতুন সাবস্ক্রিপশন শুরু করার জন্য (এটি একটি Checkout URL দিবে)। |
| `/api/v1/subscriptions/status/` | `GET` | ইউজারের বর্তমান সাবস্ক্রিপশন স্ট্যাটাস দেখার জন্য। |
| `/api/v1/subscriptions/cancel/` | `POST` | সাবস্ক্রিপশন বাতিল করার জন্য। |
| `/api/v1/subscriptions/webhook/` | `POST` | Stripe Webhook (Stripe থেকে তথ্য রিসিভ করার জন্য)। |

---

## ২. ব্যবহারের নিয়ম (How to Use)

### ধাপ ১: প্ল্যান সিলেক্ট করা
প্রথমে `/api/v1/subscriptions/plans/` থেকে আপনার পছন্দের প্ল্যান আইডিটি নিন।

### ধাপ ২: চেকআউট রিকোয়েস্ট
পেমেন্ট করার জন্য নিচের মতো করে রিকোয়েস্ট পাঠান:
- **Body:** `{"plan_id": 3}` (আইডি অনুযায়ী পরিবর্তন হবে)
- **Header:** `Authorization: Bearer <your_token>`

এটি আপনাকে একটি `checkout_url` দিবে। ওই ইউআরএল-এ ভিজিট করে পেমেন্ট সম্পন্ন করুন।

### ধাপ ৩: পেমেন্ট ও সেটআপ ফি
- **প্রথমবার সাবস্ক্রাইব:** প্রথম পেমেন্টে ইউজারের কাছ থেকে **প্ল্যান প্রাইস + ৭৫০ টাকা সেটআপ ফি** কাটা হবে।
- **পরবর্তী মাস:** এরপর থেকে প্রতি মাসে শুধু প্ল্যান প্রাইস (৩০০ বা ৫০০ টাকা) কাটা হবে।

---

## ৩. সাবস্ক্রিপশন স্ট্যাটাস (Statuses)
- **ACTIVE:** পেমেন্ট সফল এবং সাবস্ক্রিপশন সচল।
- **PAST_DUE:** ইউজারের কার্ডে টাকা না থাকলে বা পেমেন্ট ফেইল করলে এই স্ট্যাটাস দেখাবে।
- **CANCELED:** ইউজার সাবস্ক্রিপশন বাতিল করলে।
- **EXPIRED:** পেমেন্ট রিনিউ না হলে বা ট্রায়াল শেষ হলে।

---

## ৪. ওয়েব হুক (Webhooks)
সিস্টেমটি অটোমেটিক সিঙ্ক থাকার জন্য নিচের ওয়েব হুকগুলো হ্যান্ডেল করে:
1. `checkout.session.completed`: প্রথমবার পেমেন্ট সফল হলে ডাটা সেভ করে।
2. `invoice.payment_succeeded`: প্রতি মাসে অটো-রিনিউয়াল হ্যান্ডেল করে।
3. `invoice.payment_failed`: পেমেন্ট ফেইল করলে স্ট্যাটাস আপডেট করে।
4. `customer.subscription.deleted`: সাবস্ক্রিপশন পুরোপুরি বন্ধ হলে ডাটাবেস আপডেট করে।

---

## ৫. টেস্টিং গাইড (Testing)

### Stripe CLI ব্যবহার করে (Local Testing):
আপনার ওয়েব হুক টেস্ট করার জন্য কমান্ড লাইনে এটি চালান:
```bash
stripe listen --forward-to http://127.0.0.1:8000/api/v1/subscriptions/webhook/
```

### টেস্ট কার্ড (Test Cards):
পেমেন্ট টেস্ট করার জন্য Stripe-এর এই কার্ডটি ব্যবহার করুন:
- **Card:** `4242 4242 4242 4242`
- **Expiry:** যেকোনো ভবিষ্যৎ তারিখ
- **CVC:** `123`

---
*দ্রষ্টব্য: এই সিস্টেমটি এখন সম্পূর্ণ অটোমেটেড। ইউজার একবার পেমেন্ট করলে পরবর্তী মাসগুলোতে টাকা অটোমেটিক কাটবে।*
